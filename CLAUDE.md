# Rogue 開発ルール

## セッション開始時に必ず実行すること

```bash
git fetch --all
git log --all --date-order --oneline -15
```

タスク指示のブランチより新しいコミットが別ブランチにある場合は、そのブランチの最新ファイルをベースに作業すること。指示ブランチへのプッシュは引き続き行ってよい。

## 1つずつコミット

**複数タスクを同時にやるな。** `1つ変更 → コミット → プッシュ` のサイクルを守れ。

## ガイドの連動更新

ゲームのアイテム・敵・罠などに変更があった場合は、必ず `ローグゲーム完全実装ガイド.xlsx`（`generate_final_guide.js` を編集して再生成）も同時に更新すること。

## 仕様・実装メモ

`SPEC.md` にゲームの仕様・ファイル構成・タイルID・スタイルの仕組みをまとめてある。作業前に確認すること。

## キーボード操作（PC）

**矢印キーでできることは、対応するテンキーでもできるようにする。**

| 矢印 | テンキー | 用途の例 |
|------|----------|----------|
| ↑ | 8 | メニューで上の選択肢 |
| ↓ | 2 | メニューで下の選択肢 |
| ← | 4 | 横選択・サブメニュー左 |
| → | 6 | 横選択・サブメニュー右 |
| 1/3/7/9 | 同番 | ダンジョン内の斜め移動（既存） |

モーダルやハブ画面の新規キー処理を書くときは `inputKeys.js` の `isKeyUp` / `isKeyDown` / `isKeyLeft` / `isKeyRight` を使う。テンキーは `e.key` が `"2"` などになるため、**必ず `e.code === "Numpad2"` 形式で判定**すること。
