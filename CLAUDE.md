# Rogue 開発ルール

## セッション開始時に必ず実行すること

作業を始める前に以下を実行し、最新コミットがどのブランチにあるかを確認する：

```bash
git fetch --all
git log --all --date-order --oneline -15
```

タスク指示で指定されたブランチより新しいコミットが別ブランチに存在する場合は、
**そのブランチの最新ファイルをベースに作業すること**。
指示ブランチへのプッシュ自体は引き続き行ってよい。
