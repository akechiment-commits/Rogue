# Rogue 開発ルール

## セッション開始時に必ず実行すること

作業を始める前に以下を実行し、最新コミットがどのブランチにあるかを確認する：

```bash
git fetch --all
git log --all --date-order --oneline -15
```

タスク指示で指定されたブランチより新しいコミットが別ブランチに存在する場合は、
**そのブランチの最新ファイルをベースに作業すること**。
指示ブランチへのプッシュ自体は引き続き行ってよい。

## Agent tool 使用禁止

**絶対に Agent tool を使うな。**

理由：
- フリーズしてタスク完了しない
- 使用料を無駄に消費する
- 返す情報が信頼できない（推測や仮定に頼る、コード検証なし）

代わりに以下のツールを直接使用すること：
- `Read` / `Edit` / `Write` → ファイル操作
- `Grep` / `Glob` → 検索
- `Bash` → コマンド実行

必要に応じてユーザーに確認するが、エージェント起動は絶対禁止。

## 「一気にやる」絶対禁止

**「一気にやります」「まとめて実装します」などと言って複数タスクを同時にやろうとするな。**

理由：
- フリーズして永久に使用量を食いつぶすだけで何も完了しない

ルール：
- **1つ変更 → コミット → プッシュ** のサイクルを繰り返せ
- 次のタスクに進むのはプッシュが完了してから
- 「あれもこれも」は絶対にやらない。地道に1つずつ。
