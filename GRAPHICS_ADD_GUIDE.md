# グラフィック・タイル追加マニュアル

## 1. スタイルごとの経路

| スタイル | 仕組み | 追加時の確認 |
|---|---|---|
| 1 シンプル | `render.js` の記号・文字・簡易描画 | 未登録IDを空白にしない |
| 2 クラシック | DawnLike画像と `DAWNLIKE_FALLBACKS` | 既存画像へのフォールバック |
| 3 リッチ | `tiles/sprites/mon1/tile_<ID>.png` | 透過PNG、縮小時の可読性 |

## 2. タイルIDを決める

- 既存IDと衝突しないIDを選ぶ。
- モンスターは3形態で同じIDを共有するのが基本。
- 床ギミック・地形・アイテム共通画像・仮想IDは用途ごとの予約範囲を確認する。
- `tilesetMap.js` の `MONSTER_SHEET_MAP`、`DAWNLIKE_FALLBACKS`、`TILE_NAMES` を必要に応じて更新する。
- `Game.jsx` のロード対象に入らないIDは画像が表示されないため、`uniqueIds` の収集経路を確認する。

## 3. 画像の作り方

- 透過が必要なキャラクター・アイテム・床オブジェクトは背景を完全に透明にする。
- 16〜32px程度まで縮小しても、輪郭・顔・主要な特徴が読めるようにする。
- 太い輪郭、少数の高コントラスト色、大きなシルエットを優先する。
- 微細な装飾、細かい陰影、複雑な背景、斜め向きだけで意味を表す形は避ける。
- 既存スタイル3画像と並べ、サイズ・余白・透過境界・視認性を確認する。

## 4. 表示確認

- 未発見・発見済み・暗いマス・水上・壁際・敵やアイテムとの重なり。
- スタイル1、2、3の切り替え。
- 図鑑、床一覧、インベントリ、アニメーション、石像報酬、敵のレベル違い。
- ブラウザのキャッシュを避けるため、必要ならアセットのバージョン値を確認する。

画像そのものの透過確認には `tests/portraitTransparency.test.js` など既存の画像テストを参考にし、ゲーム内タイルの読み込みは `MONSTER_SHEET_MAP` と実ブラウザ表示の両方で確認する。

## 5. テストと資料

- タイルIDが定義され、画像ロード対象に含まれる。
- フォールバック画像が存在する。
- 描画時に例外や空白が出ない。
- `SPEC.md` のビジュアルスタイル章を更新する。
- 新しい敵・アイテム・罠・床ギミックなら、カテゴリ別マニュアルと完全実装ガイドも更新する。

参考ファイル: `render.js`、`useGameRenderer.js`、`tilesetMap.js`、`tileSprites.js`、`art-preview/mon1-objects-items/README.md`。
