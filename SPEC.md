# ローグゲーム 仕様書

> このファイルはゲームの仕様・実装の概要メモ。  
> アイテム詳細は `items.js`、敵詳細は `monsters.js`、罠詳細は `traps.js` を参照。  
> 開発ルールは `CLAUDE.md` を参照。

---

## ゲーム概要

React + Vite 製のローグライク。  
2Dキャンバス描画、ターン制、ランダム生成ダンジョン。  
モバイル・PCどちらにも対応。

### アイテム生成（レア度）

- 共通: `pickLootFromPool(pool, context)`（`items.js`）
- **レア度と weight は 1:1**（同レアは同 weight）
  | レア | weight | 目安 |
  | E | 12 | 最コモン |
  | D | 8 | |
  | C | 4 | |
  | B | 2 | |
  | A | 1 | |
  | S | 0.05 | 願い級のみ |
- 基本は `weight` 抽選。店・変化・ドロップは **運枠**（`LOOT_LUCK`）
  - shop / change: 18% で C 以上のみ weight
  - drop: 10% で D 以上のみ weight
  - floor: 運枠なし
- 敵の一般ランダムドロップ率: 特技なし・分裂 2% / 特技持ち 5%

### 床ギミック（拾えない固定物）

- 実装: `fixtures.js`（生成は `dungeon.attachFloorGimmicks`）
- **偽階段**: 罠データ。未看破時は階段タイルに見える。踏むとランダムな通常罠に化けて発動。看破・杖・薬は通常罠と同様
- **風穴**: `dg.vents`。中心±2 の 5×5 で**物理**飛び道具の進行方向を風向きに上書き（遠投中も同様）。対象: 投擲・矢・石（ワッカ含む）・ブレスなど。`stepProjectile(..., { wind: true })`。**杖・魔法弾は曲がらない**（`wind: false`）
- **石像**: `dg.statues`。**ダメージ／有害効果**（投擲・矢・敵の薬投げ／水鉄砲／墨／炎・氷ブレス・銃弾・杖弾など）で破壊→ややレアなアイテム＋**そのフロアの出現敵プールから1体（出現レベル+1、最大3）**。ただし**爆発で破壊された場合は粉々になり、アイテムも敵も出ない**。通行不可。**アイテム・罠・泉・大箱・魔方陣・油など床オブジェクトは重ならない**（`placeItemAt` は隣へ、罠生成・落とし穴等も石像マスを避ける。石像移動時は `displaceObjectsFromStatue`）。**移動で突っ込む／Z・Enterで正面調べは「石像がある。」表示のみ・ターン消費なし**（近接では壊さない）。**場所替え・テレポート・飛びつき**は壊さず効果を発揮（位置交換／石像を飛ばす／前に飛びつく）。**穴掘り・軟化**で破壊時は**アイテムのみ**（敵は出ない）。実装: `fixtures.hitStatueWithAction` / `wandEffectBreaksStatue` / `wandEffectStatueLootOnly`
- **固定転送**: `pentacles` の `kind:"fixed_portal"`。生成時ペア固定。ペンのポータルとは非接続。プレイヤー・敵・投げ物も転送。乗ってのかすれ消耗はしない（爆発・油・薬液など破壊経路は対象）

### 転倒の罠（trip_trap）

- レア D（weight 8）
- 踏むと **3～8 ダメージ** と「転んでしまった」メッセージ
- 未装備の所持から **2～4 個**（足りなければ全部）ランダムに周囲へ落とす（`type:"goal"` と装備中の武器・防具・指輪は除外）
- 落ちたアイテムは `placeItemAt` 経由 → **先の罠作動・泉・深い水** に通常どおり作用
- 立ち絵: メッセージで `reaction_fall` グループを表示

### ダメージ計算（攻撃 vs 防御）

- 実装: `utils.js` の `calcAtkDefDmg(atk, def, opts)`
- 基本: `floor(atk² / (atk + floor(def × weight)))`（従来どおり）
  - 敵→プレイヤー: `weight = 1.5`
  - プレイヤー→敵・矢など: `weight = 1`
- **防御の割合軽減（ソフト）**: その後 `× (1 - min(0.40, def/(def+80)))`
  - 例: 防15 ≈ 16%減 / 防30 ≈ 27%減 / 防80以上で最大40%減
  - 高 atk ボスでも防具差が出るが、防御だけで無敵にはならない
- 最後に乱数 ±2（一部の敵同士攻撃は ±1）

### 敵同士の撃破

- 敵が別の敵を倒した場合も共通の撃破処理を通り、撃破者が1レベルアップする（次形態がない場合は強化）。
- 水鉄砲・突進・ブレス・投擲・杖など、敵が敵へ与える各種攻撃もこの判定を使用する。
- 撃破された敵の通常ドロップも発生する。プレイヤーへの経験値は入らない。

### 水上限定モンスター

- わてり系（`waterOnly`）は通常の追跡・詰まり脱出・状態異常による自発移動でも、水タイルまたは泉以外へ移動しない。
- 場所替え・吹き飛ばし・引き寄せ・テレポートなどの強制移動で水の外へ出ることはある。その場合は自発的に戻ろうとせず、水の外では行動しない。

### 願い（Wish）

- 実装: `wish.js`（コア）、`WishModal`（UI）
- **泉**: 「飲む」「浸す」各約0.5%で発動。叶うと泉は必ず干上がる
- **願いの杖** (`effect: wish`): 常に1回。充填・合成で回数増加不可。生成 weight 0.05（超低確率）。振ると願いUI
- **願いの壺** (`potEffect: wish_pot`): 物を入れると願いUI。捧げた物と壺は消滅（1回限り）。生成 weight 0.05（超低確率）
- UI:
  - **直接入力**: 図鑑未登録の道具も可。拒否は全能キラー・アルテマソード・願いの杖/壺・キー類など。ゴッドスパークは可
  - **お金**: 「お金」「金」「金貨」等 → 10000〜20000G ランダム
  - **大箱**: 名前で願い可。足元優先で配置（空いていなければ DRO）
  - **道具**: 図鑑（セーブ＋今回の探索）に載った道具・大箱のみ。カテゴリ選択
  - **恩恵**: 全回復（満腹含む）／全識別／フロアを見通す／レベル+3／敵の全滅（通常撃破扱い・経験値とドロップあり／復活の魔方陣でも蘇らない）

### ダンジョン種別

| ID | 名称 | 階数 | 特記 |
|---|---|---|---|
| `tutorial` | チュートリアル | 5F | 固定構成。ローグライクの基本を学ぶ |
| `beginner` | 初心者ダンジョン | 10F | 全識別・祝呪既知 |
| `intermediate` | 中級者ダンジョン | 20F | 標準難度 |
| `advanced` | 上級者ダンジョン | 30F | 高難度 |
| `legend` | 超上級者ダンジョン | 50F | 最高難度。深淵の禁書を持ち帰るのが目標 |
| `debug` | デバッグダンジョン | 無制限 | 全アイテム・全モンスター配置。B1Fは敵部屋へ通路あり。HP/MP 200 |

`beginner` では `allBcKnown = true`（祝呪・識別状態が全てわかる）。

#### 通常フロアのレイアウト揺らぎ

- ボス／特殊フロアの抽選に入らなかった通常フロアは、約62%が従来型、残りが変則型になる。
- `wideRooms`: 部屋を広めにし、既存の部屋間接続の一部を2マス幅にする。
- `centralCross`: マップ中央を3マス幅の縦横十字路で横断し、近い部屋へ2マス幅の接続を追加する。
- `courtyard`: 中央に14×8の広場を作り、近い部屋へ2マス幅の放射状接続を追加する。
- 変則型も通常フロアの敵・アイテム・罠・店・隠し部屋の生成規則を使い、特殊フロアの扱いにはしない。生成結果には判別用の `layoutVariant` を持つ。

#### 特殊フロア：迷路の廊下

- `genCorridorFloor` が生成する `floorType: "corridorFloor"` の特殊フロア。通常フロアの部屋構成とは別に、外周壁を残した細い廊下の迷路を作る。
- ノード間隔2マスの約29×14ノード格子を再帰バックトラッキングで掘り、基本の完全迷路にする。生成後、実際に壁として残っている辺から10〜18本だけ追加で開けるため、一本道だけでなくループ・分岐・複数の行き止まりが生じる。
- 上り階段・下り階段の周囲だけ3×3の退避空間にし、さらに3〜5個の行き止まりノードを配置用の小部屋として扱う。迷路全体を広げず、罠・泉・大箱はこれらの小部屋、敵・アイテムは廊下全体に配置する。

#### 特殊フロア：ミニルーム

- `genMiniRoom` が生成する `floorType: "miniRoom"` の1部屋フロア。マップ60×30では部屋を12×8マスに抑え、階段と敵・アイテムが密集する超小型の空間にする。

### 拠点（Hub）と冒険準備

- 拠点データは `SaveData.js` の `roguelike_hub_v1` に保存する。プレイヤー名、好きな食べ物、所持金、倉庫、図鑑、クリア履歴、累計挑戦回数、最高到達階、最高獲得金額、ショップ在庫を管理する。
- プレイヤー名と好きな食べ物は1〜12文字。好きな食べ物の初期値は「おにぎり」。冒険開始時に「満腹の特盛り${favoriteFood}」を1個支給する。
- 初回は名前、好きな食べ物の順に入力したあと、`スタイル3:リッチ`／`スタイル2:クラシック`／`スタイル1:シンプル`からグラフィックスタイルを選択する。選択は `SaveData.js` の `graphicStyle` と `roguelike_tileset` に保存する。
- 冒険開始時、空きがあればショートソードと革の鎧を初期装備として支給・装備する。拠点倉庫の初期上限は100個で、500Gで50個ずつ拡張できる。倉庫が上限超過中は新しい冒険を開始できない。
- 帰還時は生還なら所持金を100%、死亡なら50%引き継ぐ。倉庫に戻すアイテム、図鑑、クリア履歴なども結果に応じて更新する。

### スコア・タイム・オンラインランキング

- ランのスコアは「プレイヤー所持金 + インベントリ内アイテムの売値合計」。スコアには階数、レベル、ターン数、経過時間、所持金、アイテム価値、生還・クリア状態を記録する。
- 経過時間は実時間で計測し、タブが非表示・アプリがバックグラウンドの間は加算しない。死亡またはクリアで停止し、セーブから復元する。
- オンラインランキングはスコア（降順）とクリアタイム（昇順）の2種類。対象ダンジョンは `beginner` / `intermediate` / `advanced` / `legend`。チュートリアルとデバッグは対象外とする。
- ランキングAPIが未設定または通信できない場合も、ローカルの冒険・結果表示は継続できる。サーバー側ではプレイヤー単位の送信間隔制限を設ける。

### 足元オブジェクトとフロア一覧

- `f` キーで現在階のフロア一覧を開く。アイテム・罠に加えて、風穴、魔方陣、泉、大箱、階段、骨などを一覧化する。
- 未識別の魔方陣は「正体不明の魔方陣」、未開示の大箱は「謎の大箱」と表示する。骨は説明用の記録で、上に乗って移動する対象ではない。
- フロア上のオブジェクトは、床・像・プレイヤー・モンスター・アイテム・他の罠やオブジェクトと重ならない位置に配置する。風穴・魔方陣・罠・階段はワープ・移動対象として扱う。

### 大箱：ゴミ箱

- ゴミ箱に入れたアイテムは消滅し、容量を1消費する。容量超過・爆発・杖など、壊れた理由を問わず破壊時に共通の大箱破壊処理を通る。
- ゴミ箱が壊れると、入っていた中身を散らすほか、変化の大箱と同じ抽選表からランダムなアイテムを1個生成して散らす。
- 呪われていて外せない装備品もゴミ箱には入れられる。ゴミ箱以外の箱では従来どおり入れられない。
- 帯電毛玉（`charged_fuzzball`）はカラペン系が押し付ける敵専用アイテム。インベントリから置く・投げる操作はできないが、箱・壺には収納できる。箱・壺の破壊や中身の散乱で床へ出た場合は、床に残らずその場で消滅する。

### 追加された敵・罠・装備効果

- 足払い鬼（`tripper`）は主に9〜24階に出現し、隣接時に一定確率で足払い攻撃を行う。
- カラペン（`itempusher`）は中級11〜18階・上級9〜22階、パタペン／ゴゴペンは上級の後半に出現する浮遊ペンギン。3種ともスタイル3では共通のタイル183を使い、隣接時に所持枠の空きがあれば50%で帯電毛玉を押し付ける。ゴゴペン（Lv3）は非隣接時、10マス以内の8方向直線上へ帯電毛玉を投げる（命中率80%）。命中すると帯電毛玉が所持品に入るが、外れた弾は消滅する。
- ものまね師（`mimic`）は主に12〜50階に出現する。隣接するモンスターの発動条件を満たすスキルを50%の確率でコピーし、移動フェーズで予約したスキルを攻撃フェーズで実行する。
- 夢喰い（`dreamEater`）は中級16〜20階・上級13〜24階を中心に出現する。近くの睡眠中モンスターを起こし、起こした夢を喰らって自身のHPを回復する。プレイヤーが睡眠中に隣接している場合は通常の2倍打撃を行い、与えたダメージ分だけ自身のHPを吸収する。
- アイテムモドキ（`itemMimic`）は床のアイテムに化けて待機し、プレイヤーが拾おうとした瞬間に正体を現してその場で攻撃する。化けている間は描画・占有判定とも床アイテムを優先し、起動したターンの敵フェーズでは追加行動しない。
- 転倒の罠（`trip_trap`）はDランク。踏むと3〜8ダメージを受け、「転んでしまった」と表示され、装備していないインベントリアイテムを2〜4個落とす。目的アイテムと装備中の武器・鎧・指輪は落とさない。落下物は罠・泉・深い水などの地形効果を受ける。コアの指輪で転倒・強制移動を無効化できる。
- 再生の指輪（`regen_ring`）は装備1個につき自然回復量を+1し、複数装備で効果が加算される。空腹増加の副作用はない。
- コアの指輪（`core_ring`）は転倒、ノックバック、引き寄せ、罠による投げ飛ばしなどの強制移動を防ぐ。
- 腹持ちの胴は革の鎧3個から特殊合成でき、空腹速度を下げる。腹持ちの指輪など、他の空腹軽減効果と重複する。

### 状態異常と復活抑制

- 睡眠、麻痺、鈍足、混乱、暗闇、魅了、封印などの状態異常にはプレイヤー・通常モンスター・ボスごとの持続時間を設定する。祝福された効果は有限時間の持続を2倍にする場合がある。
- 封印中のモンスターは特殊能力・固有特性・反射などが無効化され、通常速度・最大1回攻撃として扱う。
- ボスの封印は解除されたターンから通常行動できる。睡眠・金縛りは解除されたターンには行動しない。
- 呪われた復活の魔方陣は、同じマス（部屋内では同じ部屋）における復活を抑制する。対象はMPによる復活、通常の復活魔方陣、骨などの復活手段を含む。

---

## ビジュアルスタイル

### 3種類のタイルセット

| スタイル番号 | 名称 | 概要 | 保存場所 |
|---|---|---|---|
| 1 | シンプル | 生成タイルと文字フォールバック中心 | `render.js` の `TILE_RENDER` |
| 2 | クラシック | DawnLikeの16pxタイル画像 | `tiles/sprites/dawnlike/` と `public/tiles/` |
| 3 | リッチ | モンスター・アイテムごとに1枚ずつのPNG | `tiles/sprites/mon1/tile_${id}.png` |

スタイルは `currentTileset` ステートで管理（`Game.jsx`）。初回設定とゲーム内の設定画面から切り替えられる。
右サイドバーの立ち絵はスタイル3（リッチ）のときだけ表示し、スタイル1（シンプル）・スタイル2（クラシック）では非表示にする。設定画面で切り替えた場合も即時反映する。
スタイル1で個別画像を持たない敵IDには、`render.js` の識別用文字を表示する。将来追加される未登録IDも `?` で表示し、空白描画にしない。

### スタイル3のファイル配信

`tiles/` はルートにありViteの `public/` 配下ではないため、`vite.config.js` のカスタムプラグインで配信している。  
スタイル2はDawnLikeのID画像を先に読み込み、足りない追加IDは `public/tiles/{名前}.png` と既存DawnLikeタイルのフォールバックで埋める。スタイル3とは別の読み込み経路を使う。

### バーチャルタイルID（スタイル3専用）

アイテムの外見を冒険ごとにランダム化するために使う仮想ID。  
画像は `Game.jsx` の `useEffect([currentTileset])` ブロックで選択中のスタイルに応じて `customTileImages[idx]` に読み込まれる。スタイル2に対応画像のない仮想敵IDは `tilesetMap.js` の `DAWNLIKE_FALLBACKS` を使う。

| 範囲 | 用途 |
|---|---|
| 2001〜2009 | ペン（`tiles/items/item_r01_c01〜c09.png`） |
| 3001〜3025 | 薬（薬スプライトプール参照） |

スタイル3の地形専用タイルIDは次のとおり。`T.WALL` のうちマップ端（`x=0`、`y=0`、`x=MW-1`、`y=MH-1`）だけ外周壁画像を使い、内部の通常壁とは描き分ける。壊せる壁と水路は、画像が読み込まれている場合に記号ベースのフォールバック表示を置き換える。

| タイルID | 用途 | ファイル |
|---|---|---|
| 134 | 壊せる壁 | `tiles/sprites/mon1/tile_134.png` |
| 135 | 外周壁 | `tiles/sprites/mon1/tile_135.png` |
| 136 | 水路 | `tiles/sprites/mon1/tile_136.png` |

オブジェクト用のスタイル3タイルは、階段が2/3、泉が31、大箱が38（鍵・金具のない単純な木箱）、魔方陣が74、骨が107、石像が129、固定転送陣が130。風穴（128）はスタイル3のみ風向き別スプライト（仮想ID 194〜201）を使い、矢印と風の流線で8方向を表示する。スタイル1・2では従来のキャンバス矢印フォールバックを使う。アイテムカテゴリの共通画像は仮想ID 4001〜4003を使い、薬・空き瓶・ペンの冒険内ランダム画像より優先して表示する。

| タイルID | 用途 | ファイル |
|---|---|---|
| 4001 | 薬カテゴリ | `tiles/sprites/mon1/tile_4001.png` |
| 4002 | 空き瓶 | `tiles/sprites/mon1/tile_4002.png` |
| 4003 | ペンカテゴリ | `tiles/sprites/mon1/tile_4003.png` |

敵のうち、既存の汎用画像では名前との不一致が大きかったもの、同じ画像を共有していたものはスタイル3専用ID 145〜180へ分離している。カラペン系は共通の専用ID 183（`tiles/sprites/mon1/tile_183.png`）を使う。対応は `monsters.js` と `tilesetMap.js` を参照する。

**スタイル1/2では画像が読み込まれないため、レンダラーで必ず `customTileImages[vi]` の存在チェックをしてフォールバックすること。**

### バーチャルタイルIDを新規追加する手順

1. `Game.jsx` の `useEffect([currentTileset])` に画像読み込みを追加（mon1 専用）
2. `useGameRenderer.js` の `_spriteTile` 系ヘルパーを更新（`customTileImages[vi]` チェック必須）
3. `Game.jsx` の `init()` と resume パスにスプライトマップを追加
4. `GameSave.js` の save/load 両方に追加

スタイル3のみ、ペン・薬のグラフィックが冒険ごとにランダム化される。

---

## アイテムのタイルID一覧

`items.js` で `tile` プロパティに設定されている値。スタイル3の場合 `tiles/sprites/mon1/tile_${id}.png` が使われる。

| tile ID | アイテム種別 | スタイル3 画像の内容 |
|---|---|---|
| 16 | 薬（基本）| → potionSpriteMap でランダム化（3001〜3025） |
| 17 | 薬（上位）| → 同上 |
| 18 | 巻物・特殊巻物 | — |
| 19 | 食料（生） | 果物・葉のまとまり（`item_food_raw_fruit.png`） |
| 20 | 武器 | chip3_v2/item_r01_c05 |
| 21 | 防具 | chip3_v2/item_r04_c01 |
| 22 | 金貨 | — |
| 23 | 矢 | chip3_v2/item_r11_c01 |
| 24 | 杖 | 青紫の大きな発光宝石＋星形の魔力（`item_wand_flashy.png`） |
| 25 | 地雷（罠） | chip3_v2/item_r13_c03 |
| 26〜30, 45〜51, 71〜73, 84〜85, 94 | 各種罠（地雷以外） | tile_25.png と同じ画像 |
| 133 | 転倒の罠 | 専用マップアイコン |
| 32 | 壺 | — |
| 41 | 魔法の筆（マーカー） | — |
| 42 | ペン（基本）| → penSpriteMap でランダム化（2001〜2009） |
| 43 | 魔法書 | — |
| 60 | 指輪 | chip3_v2/item_r06_c01 |
| 66 | 食料（調理済み） | 皿・大きな卵料理・赤いソース・葉の一皿料理（`item_food_cooked_plated_v3.png`） |
| 87〜92, 101〜102, 184〜189 | 宝石類 | スタイル3では宝石ごとの固有画像 |
| 190〜193 | キーアイテム | 輝く宝玉・深紅の魔石・伝説の王冠・深淵の禁書（固有画像） |

### ペンスプライトプール（2001〜2009）

`tiles/items/item_r01_c01.png` 〜 `item_r01_c09.png`（9枚）。  
`penSpriteMap: { effect名: 1〜9 }` で管理。

### 薬スプライトプール（3001〜3025）

```
1=item_r04_c07  2=item_r04_c10  3=item_r04_c11  4=item_r04_c14
5=item_r05_c01  6=item_r05_c02  7=item_r05_c03  8=item_r05_c04
9=item_r05_c05  10=item_r05_c06 11=item_r05_c08 12=item_r05_c12 13=item_r05_c14
14=item_r03_c01 15=item_r03_c02 16=item_r03_c03 17=item_r03_c04
18=item_r03_c05 19=item_r03_c15
20=item_r04_c01 21=item_r04_c02 22=item_r04_c03 23=item_r04_c04
24=item_r04_c05 25=item_r04_c06
```

`potionSpriteMap: { effect名: 1〜25 }` で管理。

---

## アイテム種別と識別システム

### 識別が必要なアイテム

未識別時は偽名（fakeNames）で表示される。識別キー：

| 種別 | 識別キー形式 | 例 |
|---|---|---|
| 薬 | `p:${effect}` | `p:heal` |
| 巻物 | `s:${effect}` | `s:teleport` |
| 指輪 | `r:${effect}` | `r:power_ring` |
| 杖 | `w:${effect}` | `w:sleep` |

### アイテム種別一覧

| type | 説明 |
|---|---|
| `potion` | 薬。飲む・投げる効果あり |
| `scroll` | 巻物。読む効果あり |
| `weapon` | 武器。装備可能 |
| `armor` | 防具。装備可能 |
| `arrow` | 矢。99本まで束 |
| `ring` | 指輪。最大2個装備。＋値あり |
| `wand` | 杖。充填あり |
| `pen` | ペン。足元に魔方陣を描く。充填2 |
| `marker` | 魔法の筆。白紙の巻物に魔法を書ける |
| `scroll`（特殊）| 識別・複製・換金・変成など |
| `spellbook` | 魔法書。魔法を習得できる |
| `food` | 食料。満腹度回復 |
| `pot` | 壺。アイテムを入れて使う |
| `gem` | 宝石。店限定。通常店にも低確率でC以上が出現し、宝石専門店では出やすくなる。遠方の店で高値で売れる |
| `gold` | 金貨 |
| `bottle` / `potion(water)` | 空き瓶・水 |

### 祝福・呪いシステム

- `bc: "blessed"` / `bc: "cursed"` / なし（normal）
- 祝福：効果強化、呪い：効果反転または弱体化
- 未識別の場合、祝呪も不明

---

## 罠一覧

`traps.js` で定義。tile ID 25 が地雷（特別）、それ以外は全て同じ画像（tile_25と同じ）。

| tile | effect | 名称 |
|---|---|---|
| 25 | `land_mine` | 地雷（爆発） |
| 26 | `arrow_trap` | 矢の罠 |
| 27〜30 | 各種 | 毒・炎・石化・落とし穴 等 |
| 45 | `invisible_trap` | 透明の罠 |
| 46 | `summon_trap` | 召喚の罠 |
| 47 | `slow_trap` | 鈍足の罠 |
| 48 | `seal_trap` | 封印の罠 |
| 49 | `steal_trap` | 盗みの罠 |
| 50 | `hunger_trap` | 空腹の罠 |
| 51 | `blowback_trap` | 吹き飛ばしの罠 |
| 71〜73 | 各種 | — |
| 84 | `bewitch_trap` | 惑わしの罠 |
| 85 | `darkness_trap` | 暗闇の罠 |
| 94 | `rot_trap` | 腐敗の罠 |
| 133 | `trip_trap` | 転倒の罠 |

地雷（`explode`）をアイテムが直接起動した場合、その起動アイテムは爆発で消滅し、発動後に床へ再配置しない。重力などの内部トリガーはアイテムとして扱わない。

---

## ゲームセッションの状態構造（session / gs）

`Game.jsx` の `gs` ステートに入る主要フィールド：

```js
{
  player: { playerName, ... }, // プレイヤーデータ
  dungeon: { ... },         // 現在フロアのダンジョンデータ
  floors: { 1: dg, ... },   // 各フロアのキャッシュ
  ident: Set<string>,       // 識別済みキーのセット
  fakeNames: { ... },       // 偽名テーブル
  bbFakeNames: { ... },     // 大箱偽名テーブル
  nicknames: { ... },       // ユーザーが付けたニックネーム
  isDebugRun: bool,
  dungeonType: string,      // "beginner" 等
  maxDepth: number | null,
  allBcKnown: bool,
  floorTurns: number,
  runActiveMs: number,
  penSpriteMap: { effect: 1〜9 },    // 冒険ごとにランダム化
  potionSpriteMap: { effect: 1〜25 }, // 冒険ごとにランダム化
}
```

### 再現可能な乱数

`utils.js` の `createSeededRng(seed)` は、ダンジョン生成・抽選のテストで注入する 0 以上 1 未満の乱数関数を返す。`rng` / `pick` / `shuffle` は第2または第3引数でその関数を受け取れる。ゲーム本体の乱数呼び出しは段階的にこの注入形式へ移行する。

---

## 主要ファイル構成

| ファイル | 役割 |
|---|---|
| `Game.jsx` | メインゲームロジック・状態管理 |
| `useGameRenderer.js` | キャンバス描画ループ |
| `render.js` | `drawTile()`・スタイル1描画定義・`customTileImages` |
| `dungeon.js` | ダンジョン生成ロジック |
| `items.js` | 全アイテム定義・識別・フェイク名テーブル |
| `lootRules.js` | レア度・重み付き抽選・運枠・一般ドロップ率の純粋ルール |
| `inventoryRules.js` | インベントリの種別順・名前順ソート規則 |
| `inventoryLabel.js` | インベントリの装備・識別・能力値を含む表示ラベル生成 |
| `turnUpkeep.js` | プレイヤーの基礎更新・状態タイマー・強化・行動不能ターン・闘気・魔方陣消耗 |
| `playerTerrainEffects.js` | 壁抜け解除・壁埋まり・深水・泉での壺閉じ込めによる毎ターン地形効果 |
| `playerPentacleEffects.js` | 呪われた聖域と雷の魔方陣について、プレイヤー足元で起きる毎ターン効果 |
| `monsterTurnAnimation.js` | モンスター移動・攻撃のアニメーション用スナップショットとイベント記録 |
| `monsterUpkeep.js` | 攻撃後のボス回復・敵の状態タイマー・雷の魔方陣の敵への毎ターン効果 |
| `turnHazards.js` | 敵移動後・敵攻撃前の遅延地雷・時限爆弾・回転板・爆発の指輪の解決 |
| `monsterPortalTransit.js` | 敵移動後の固定転送陣・ポータル転送（呪い・別フロア移動を含む） |
| `monsterAttackPhase.js` | 敵攻撃フェーズの実行と、命中・回避・突進の描画イベント生成 |
| `visibilityOverrides.js` | 明かりの魔方陣とスターライト／ダークネスによる refreshFOV 後の視界上書き |
| `pentacleTurnEffects.js` | 転送・罠生成・石飛ばし・回復の魔方陣による毎ターン効果 |
| `fixtureQueries.js` | 石像・固定転送陣の副作用を持たない照会と杖効果判定 |
| `monsterRuntime.js` | アイテム処理から敵実装を循環なしで呼ぶための実行時ポートと休眠解除ルール |
| `monsters.js` | 全モンスター定義・AI |
| `traps.js` | 罠定義・発動ロジック |
| `GameSave.js` | セーブ・ロード（localStorage） |
| `GameHelpers.js` | 戦闘・アイテム使用などのヘルパー |
| `lookDescription.js` | 調べるモードのマス説明を組み立てる純粋ロジック |
| `messageLog.js` | メッセージのターン付与・履歴追加を正規化する純粋ロジック |
| `GameModals.jsx` | インベントリ・各種モーダルUI |
| `GameButtons.jsx` | モバイル用ボタン |
| `HubScreen.jsx` | タイトル・ダンジョン選択画面 |
| `SaveData.js` | 拠点セーブの読み書き、拠点状態の初期値・正規化 |
| `playerLabel.js` | プレイヤー名の正規化・検証、ランキング用IDの生成 |
| `favoriteFood.js` | 好きな食べ物の正規化・検証、冒険開始時の特盛り食料生成 |
| `runScore.js` | ランのスコア・所持金・アイテム価値・結果情報の算出 |
| `runTimer.js` | タブ表示中だけ進む実時間タイマーとセーブ復元 |
| `rankingClient.js` | ランキングの取得・自分の記録取得・結果送信 |
| `api/ranking.js` | オンラインランキングAPI。スコア・クリアタイム・レート制限を管理 |
| `floorInventory.js` | 階ごとのアイテム・罠・床オブジェクト一覧 |
| `floorObjectPlacement.js` | フロアオブジェクトの占有判定・配置・階段移動 |
| `monTraits.js` | モンスター固有特性と封印時の無効化判定 |
| `statusDuration.js` | 状態異常の持続時間、祝福・対象別の補正 |
| `revivalRules.js` | 呪われた魔方陣による復活抑制の共通判定 |
| `tilesetMap.js` | スプライトシートのマッピング定義 |
| `animEvents.js` | アニメーションイベントシステム |
| `DiscoveryTracker.js` | 図鑑・発見追跡 |
| `vite.config.js` | Viteカスタムプラグイン（tiles/配信） |
| `generate_final_guide.js` | Excelガイド生成スクリプト |
| `process_items_bg.py` | アイテム画像の透過処理スクリプト |

### 責務分離マップ（2026-07-25 時点）

大きな UI ファイルにゲームルールを再び戻さないための対応表。`Game.jsx` は状態更新の順序・React UI・外部依存の受け渡しを担当し、下記モジュールは UI 非依存のルールを担当する。

| 元の置き場 | 移設先 | 分離した担当 |
|---|---|---|
| `Game.jsx` | `turnUpkeep.js` | 空腹・自然回復、MP、毒、基本状態タイマー、食料由来の一時効果、行動不能／自動スキップ、闘気防具、魔方陣消耗、等速・鈍足・倍速の速度フェーズ |
| `Game.jsx` | `playerTerrainEffects.js` | 壁抜け終了時の押し出し、壁埋まり、深水・泉上の壺によるダメージ |
| `Game.jsx` | `playerPentacleEffects.js` | 呪われた聖域の即死、雷の魔方陣の雷撃・回復・打ち消し・所持品への雷 |
| `Game.jsx` | `monsterTurnAnimation.js` | モンスター移動前後の差分、等速敵の移動済み印、攻撃フラッシュ・命中・突進の描画データ |
| `Game.jsx` | `monsterUpkeep.js` | ボス自然回復、油／浮遊の終了、雷の魔方陣による敵の回復・ダメージ・撃破 |
| `Game.jsx` | `turnHazards.js` | Phase 3 の爆発の指輪、遅延地雷、時限爆弾、遅延回転板と攻撃スキップ判定 |
| `Game.jsx` | `monsterPortalTransit.js` | Phase 2.5 の固定転送陣・ポータルに乗った敵の転送と、着地後の再転送防止 |
| `Game.jsx` | `monsterAttackPhase.js` | Phase 4 の敵攻撃実行と、アニメーションへ渡す命中・回避・突進データ |
| `Game.jsx` | `visibilityOverrides.js` | 明かりの魔方陣と視界操作モンスターの視界・探索済みマスへの上書き |
| `Game.jsx` | `pentacleTurnEffects.js` | 転送・罠生成・石飛ばし・回復の魔方陣による毎ターン効果 |
| `Game.jsx` | `lookDescription.js` | 調べるモードで表示するマスの説明文 |
| `Game.jsx` | `messageLog.js` | メッセージへのフロアターン付与と履歴上限の管理 |
| `Game.jsx` | `inventoryRules.js` / `inventoryLabel.js` | インベントリの利用可否・使用ラベル・ソート・表示名 |
| `items.js` | `lootRules.js` | レア度／重み抽選、運枠、一般ドロップ抽選 |
| `fixtures.js` | `fixtureQueries.js` | 石像検索、固定転送陣のペア検索、杖による石像破壊判定 |

各分離先には対応する `tests/*.test.js` を置く。挙動変更を伴わない整理でも、既存のメッセージ・処理順・副作用をテストで固定してから `Game.jsx` 側を呼び出しだけにする。

`fixtures.js` は `items.js` を import しない。偽階段の実体化や石像破壊で必要なアイテムカタログ・名前解決は、呼び出し側が `getFixtureItemDeps()` で渡して依存方向を `items → fixtures` に固定する。

`items.js` は `wands.js` を import／再exportしない。杖破壊時の効果は `wands.js` が `setWandBreakEffectHandler()` で登録し、UIは杖APIを `wands.js` から直接importする。モンスター投擲に必要な杖効果も `monsterAI` の `applyWandFn` として呼び出し側から渡す。

`items.js` は `monsters.js` を import しない。敵生成・レベル変化・弾道解決などは `monsters.js` が `monsterRuntime.js` へ登録し、アイテム処理は実行時ポート経由で呼ぶ。ルート実装モジュールの循環依存は `tests/moduleDependencies.test.js` で0件を固定する。

敵の金縛り・睡眠・移動封じ中は、AIの詰まり脱出処理でも座標を動かさない。重力の魔方陣が敵に罠を踏ませる際の「重力の力」は内部トリガーであり、床アイテムとして生成・取得されない。

### スプライト画像の場所

```
tiles/
  sprites/
    mon1/           ← スタイル3メインスプライト（tile_${id}.png）
  items/            ← ペン・薬のランダムグラフィックプール
  chip3_v2/         ← 武器・防具・指輪等の素材（切り出し前）
  Character/        ← プレイヤー立ち絵（81枚、状況別PNG）
public/
  tiles/            ← スタイル2（DawnLike）スプライト ※優先度高
```

---

## キャラクター立ち絵システム

2026-07-26 に `tiles/Character` 直下の全立ち絵を確認し、黒・白・グリーンバックのうち四隅から連結する背景だけを透過済み。
再処理には `npm run portrait-transparent` を使う。色差 threshold=20 のフラッドフィルなので、キャラクター内部の黒い衣装や影、白いエフェクトは保持される。

### 立ち絵ファイル（`tiles/Character/`）

81枚のPNG。場面に応じて命名：

| プレフィックス | 内容 |
|---|---|
| `stand_*` | 通常立ち・歩行・装備 |
| `battle_*` | 攻撃モーション各種（`battle_melee`＝武器近接、`battle_unarmed`＝素手、`battle_dash`＝ダッシュ） |
| `damage_*` | 被ダメージ（矢・炎・重撃など属性別） |
| `hp_*` | HP状態（満タン・中程度・瀕死・傷） |
| `action_*` | アイテム使用（薬・食料・巻物・杖・弓・投擲・壺・宝箱） |
| `reaction_*` | リアクション（レベルアップ・驚き・喜び・落ち込み） |
| `status_*` | 状態異常（毒・睡眠・金縛り・拘束＝からめ鬼捕獲・閉じ込め＝とじこめ壺・移動不能など） |
| `gameover_dead` | ゲームオーバー画面専用 |

### 立ち絵の切り替えロジック（`Game.jsx`）

- `portraitSrc` ステート＋`setPortraitSrc` で管理
- `forcePortrait(key)` ：クールタイム無視で即切り替え（被ダメージ・死亡など）
- `tryPortrait(key)` ：4秒クールタイム内は切り替えしない（歩行・瀕死表示などの頻繁な状態変化）
- 読む・食べる・装備・攻撃・壺／宝箱操作などの能動アクションはクールタイムを無視して即時切り替え。HP回復は最大HPの20%以上かつ20HP以上の大回復時だけ「回復した」を表示する
- `PORTRAIT_SETS` オブジェクトで各 key → ファイル名の配列（ランダム選択）
- 優先度：死亡 > 被ダメージ > **拘束中（`capturedBy`）は他行動より拘束立ち絵を常時優先** > レベルアップ > 状態異常 > アイテム使用 > 歩行 > 攻撃
- HP が低いと `hp_low` 系を優先（歩行時も上書き）

### サイドバーへの表示（`GameModals.jsx` の `SidebarPanel`）

- 右サイドバー背景色 `#f5f0e8`
- 画像幅 220%・overflow: visible（意図的にはみ出し）
- ゲームオーバー画面では `gameover_dead.png` を赤いドロップシャドウ付きで大きく表示

---

## セーブデータ

`localStorage` キー: `roguelike_dungeon_save_v1`（保存形式 v2。v1 はロード時に互換変換）
ファイル: `GameSave.js`

- 装備品はインベントリインデックスで保存
- `ident` は `Set → Array → Set` で変換
- `penSpriteMap`, `potionSpriteMap` も保存・復元される
- `runActiveMs` はタブが表示されていた実時間の累積値として保存・復元される
- 直近80件のメッセージが保存される

### 拠点セーブ（`SaveData.js`）

- `localStorage` キーは `roguelike_hub_v1`、保存形式は v1。
- プレイヤー名、ランキング用の匿名 `playerId`、好きな食べ物、拠点所持金、倉庫・持ち込み在庫、倉庫上限、発見済みアイテム・モンスター・罠・大箱、識別済み効果、クリア済みダンジョン、累計挑戦回数、最高到達階、最高獲得金額、ショップ在庫を保持する。
- `playerId` はランキング送信用の識別子で、表示名やゲーム内のプレイヤー情報とは分けて扱う。

---

## 未実装・TODO メモ

（実装したら随時ここに追記）
