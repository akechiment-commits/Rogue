# 食料・料理追加マニュアル

## 1. 食料名を追加する

通常の食材は `foodData.js` の `RAW_FOODS`、料理は `COOKED_FOODS_SAVORY` または `COOKED_FOODS_SWEET` に追加する。

食料は個別アイテムを `ITEMS` に1件ずつ追加する方式ではなく、`genFood()` が名前、サイズ、料理状態、満腹値、カテゴリ、効果を組み立てる。

## 2. 食料データの確認

- 生食材か調理済みか。
- `foodCat` が味付け壺の相性カテゴリに正しく入るか。
- `RAW_SIZES`／`COOKED_SIZES` のサイズ表示と満腹値。
- 食料固有効果 `FOOD_EFFECTS`、腐敗、焦げ、薬を浴びた状態。
- 好物判定、料理名、説明文、図鑑表示。

カテゴリや特殊効果を新設する場合は、`foodData.js` のカテゴリマップと `items.js` の食料効果を同時に確認する。

## 3. 説明文

- 一般的な食料名は `foodDescriptions.js` の説明生成を使う。
- 個別説明が必要なら `foodDescriptionOverrides.js` を追加する。
- ゲーム内表示と完全実装ガイドの説明を分ける。
- `generate_final_guide.js` の食料一覧は大量データを生成するため、名前が重複しないことを確認する。

## 4. 生成・売店・特殊処理

- 通常フロア、特殊フロア、店、食料生成の魔法書、祭壇、壺効果が対象か確認する。
- 食料を薬で変化させた場合の接頭辞、value、調理状態を確認する。
- 腐敗、焦げ、焼却、満腹の大箱、味付け壺、ハーゲンダッ壺との相互作用を確認する。
- 食料は個体IDを持ち、床上での図鑑計上は同じ個体につき1回にする。

## 5. テスト

- 生／調理済み、サイズ、満腹値、カテゴリ、説明。
- 薬を浴びせる、食べる、腐る、焦げる、壺へ入れる。
- 店・特殊フロア・魔法書・セーブ／ロード。

参考テスト: `tests/foodData.test.js`、`tests/foodCreate.test.js`、`tests/favoriteFood.test.js`、`tests/klein_pot.test.js`。
